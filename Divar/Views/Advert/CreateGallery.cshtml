@model Divar.Core.ViewModels.AddGalleryViewModel

@{
    Layout = null;
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateGallery" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>                       
            
            <div class="form-group">
                <input type="file" asp-for="Img" />
                <h5>یک تصویر بارگزاری کنید</h5>
            </div>
            <div class="form-group">
                <input type="submit" value="ذخیره" class="btn-red" />
            </div>
        </form>
    </div>
</div>
